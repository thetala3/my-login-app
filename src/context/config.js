const BASE_URL = "https://localhost:44342/";

const config = {
  GetTransactionsList: BASE_URL + "GetTransactionsList/",
  LOGIN_URL: BASE_URL + "api/Auth/authenticate"
};

//window.config = config;
export default config;

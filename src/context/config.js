const BASE_URL = "https://localhost:7017/";

const config = {
  GetTransactionsList: BASE_URL + "GetTransactionsList/",
  LOGIN_URL: BASE_URL + "api/Auth/login"
};

//window.config = config;
export default config;
